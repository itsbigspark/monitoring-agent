# Definition of Done

A ticket is Done only when:

1. Acceptance criteria are verified.
2. Required tests and gates pass.
3. Documentation/specs reflect the shipped behavior.
4. Backlog, ticket, and spec status are updated where `workflow.mode` requires
   them.
5. PR or change reference is linked where the repo's lifecycle requires it.
